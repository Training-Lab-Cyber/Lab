plugin: gcp_compute
projects:
  - crafty-airfoil-438000-g9
auth_kind: application
keyed_groups:
  - prefix: gcp
    key: labels
hostnames:
  - private_ip
compose:
  ansible_connection: >
    {{ 'winrm' if 'windows' in labels else 'ssh' }}
  ansible_user: "ansible"
  ansible_password: "StrongPassword123!"
  ansible_winrm_transport: ntlm
  ansible_winrm_server_cert_validation: ignore 